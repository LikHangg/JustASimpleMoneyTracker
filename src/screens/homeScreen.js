import react from 'react';
import {SafeViewArea, View, Text} from 'react-native';

const HomeScreen = () => {
  return (
    <SafeViewArea>
      <Text>HomeScreen</Text>
    </SafeViewArea>
  );
};

export default HomeScreen;
